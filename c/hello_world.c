#include <stdio.h>
#include <stdlib.h>

int add_together(int x, int y) {
  int result = x + y;
  return result;
}

int main(int argc, char** argv) {
  puts("Hello, world!");
  // int added = add_together(10, 18);
  // char str[12];
  // sprintf(str, "%d", added);
  // sprintf()
  // puts(add_together(90, 18));
  return 0;
}
